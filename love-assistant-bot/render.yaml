services:
  - type: web
    name: love-assistant-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python love_bot.py"
    envVars:
      - key: BOT_TOKEN
        sync: false
